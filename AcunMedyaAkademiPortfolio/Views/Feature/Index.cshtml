@Model List <using AcunMedyaAkademiPortfolio.Models.TblFeature>
    @{
        ViewBag.Title = "Index";
        Layout = "~/Views/AdminLayout/Index.cshtml";
    }
    <table class="table table-bordered">
        <tr>
            <th>#</th>
            <th>Title</th>
            <th>Subtitle</th>
            <th>Image URL</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.FeatureId</td>
                <td>@item.FeatureTitle</td>
                <td>@item.FeatureSubtitle</td>
                <td align="center">
                    <img src="@item.FeatureImageUrl" style="width:150px" />
                </td>
                <td><a href="/Feature/UpdateFeature/@item.FeatureId" class="btn btn-warning">Update</a></td>
                <td><a href="/Feature/DeleteFeature/@item.FeatureId" class="btn btn-danger">Delete</a></td>
            </tr>
        }
    </table>
    <td><a href="/Feature/CreateFeature" class="btn btn-success">Add New Feature</a></td>
